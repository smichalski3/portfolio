// Define all your maps first
var blankMap = L.map('blankMap', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var blankMap2 = L.map('blankMap2', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var breedingMap = L.map('breedingMap', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var pointsMap = L.map('pointsMap', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var climateMap = L.map('climateMap', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var nonBreedingMap = L.map('nonBreedingMap', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap = L.map('iceMap', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var points2Map = L.map('points2Map', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap1 = L.map('stepMap1', {
  center: [55.0, -145.0],  // center over southeast Alaska area
  zoom: 4.5,               
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});


var stepMap2 = L.map('stepMap2', {
  center: [46.5, -125], // around WA/OR coast 
  zoom: 4,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap3 = L.map('stepMap3', {
  center: [46.5, -125], // around WA/OR coast 
  zoom: 4,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});


var stepMap4 = L.map('stepMap4', {
  center: [46.5, -125], // around WA/OR coast 
  zoom: 4,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap5 = L.map('stepMap5', {
  center: [46.5, -125], // around WA/OR coast 
  zoom: 4,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap6 = L.map('stepMap6', {
  center: [46.5, -125], // around WA/OR coast 
  zoom: 4,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap7 = L.map('stepMap7', {
  center: [32.5, -120], // mexico/Cal coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});


var stepMap8 = L.map('stepMap8', {
  center: [32.5, -120], // mexico/Cal coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap9 = L.map('stepMap9', {
  center: [32.5, -120], // mexico/Cal coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap10 = L.map('stepMap10', {
  center: [32.5, -120], // mexico/Cal coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap11 = L.map('stepMap11', {
  center: [32.5, -120], // mexico/Cal coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap12 = L.map('stepMap12', {
  center: [32.5, -120], // mexico/Cal coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap13 = L.map('stepMap13', {
  center: [32.5, -120], // mexico/Cal coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap14 = L.map('stepMap14', {
  center: [32.5, -120], // mexico/Cal coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap15 = L.map('stepMap15', {
  center: [-45, -80], // south american coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});


var stepMap16 = L.map('stepMap16', {
  center: [-45, -80], // south american coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap17 = L.map('stepMap17', {
  center: [-45, -80], // south american coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap18 = L.map('stepMap18', {
  center: [-45, -80], // south american coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap19 = L.map('stepMap19', {
  center: [-45, -80], // south american coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap20 = L.map('stepMap20', {
  center: [-45, -80], // south american coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var stepMap21 = L.map('stepMap21', {
  center: [-45, -80], // south american coast
  zoom: 3,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var blankMap3 = L.map('blankMap3', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var blankMap4 = L.map('blankMap4', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap1 = L.map('iceMap1', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap2 = L.map('iceMap2', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap3 = L.map('iceMap3', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap4 = L.map('iceMap4', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap5 = L.map('iceMap5', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap6 = L.map('iceMap6', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap7 = L.map('iceMap7', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap8 = L.map('iceMap8', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap9 = L.map('iceMap9', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});

var iceMap10 = L.map('iceMap10', {
  center: [0, 0],
  zoom: 2,
  maxZoom: 18,
  minZoom: 1,
  scrollWheelZoom: false,
  zoomControl: true
});


// Add base tile layers
const tileMaps = [blankMap, blankMap2, blankMap3, blankMap4, breedingMap, pointsMap, climateMap, nonBreedingMap, iceMap, points2Map, stepMap1, stepMap2, stepMap3, stepMap4,
  stepMap5, stepMap6, stepMap7, stepMap8, stepMap9, stepMap10, stepMap11, stepMap12, stepMap13, stepMap14, stepMap15, stepMap16, stepMap17, stepMap18,
  stepMap19, stepMap20, stepMap21, iceMap1, iceMap2, iceMap3, iceMap4, iceMap5, iceMap6, iceMap7, iceMap8, iceMap9, iceMap10
];
tileMaps.forEach(function(map) {
  L.tileLayer(
    'https://api.mapbox.com/styles/v1/smichalski/cme9a7p47006t01s28yrkeknl/tiles/256/{z}/{x}/{y}@2x?access_token=pk.eyJ1Ijoic21pY2hhbHNraSIsImEiOiJjbDl6d2s0enYwMnI1M29uMDhzNXB0NTRlIn0.c1_vy157AkEEGNIfyQI9YQ',
    {
      maxZoom: 18
    }
  ).addTo(map);
});


// tern points down styling 
function styleTernPointsDown(feature) {
  return {
    radius: 3,
    fillColor: "#34547e",
    color: "#40699e",
    weight: 1,
    opacity: 1,
    fillOpacity: 0.9
  };
}


// climate zone polygon styling 
function styleClimateZone(feature) {
  const dn = feature.properties.DN;
  const colorMap = {
    1: '#a50026',
    2: '#d73027',
    3: '#f46d43',
    4: '#fdae61',
    5: '#fee090',
    6: '#ffffbf',
    7: '#e0f3f8',
    8: '#abd9e9',
    9: '#74add1',
    10: '#4575b4',
    11: '#313695',
    12: '#800080',
    13: '#FF00FF',
    14: '#00FFFF',
    15: '#008080',
    16: '#00FF00',
    17: '#ADFF2F',
    18: '#FFD700',
    19: '#FFA500',
    20: '#FF6347',
    21: '#CD5C5C',
    22: '#8B0000',
    23: '#2F4F4F',
    24: '#708090',
    25: '#000080',
    26: '#191970',
    27: '#483D8B',
    28: '#6A5ACD',
    29: '#7B68EE',
    30: '#9370DB'
  };

  return {
    fillColor: colorMap[dn] || '#cccccc',
    color: '#4b4b4b',
    weight: 0.5,
    fillOpacity: 0.6
  };
}

// tern points up styling 
function styleTernPointsUp(feature) {
  return {
    radius: 3,
    fillColor: "#34547e",
    color: "#40699e",
    weight: 1,
    opacity: 1,
    fillOpacity: 0.9
  };
}


// GET DATA FUNCTION - Only bind popups on climateMap and stepMap[1-21], with custom classes
function getData(map, url, iconUrl, customStyle) {
  function allowPopupFor(m) {
    const id = m && m._container && m._container.id;
    return id === 'climateMap' || /^stepMap\d+$/.test(id);
  }
  function popupClassFor(m) {
    const id = m && m._container && m._container.id;
    if (id === 'climateMap') return 'popup-climate';
    if (/^stepMap\d+$/.test(id)) return 'popup-step';
    return '';
  }

  fetch(url)
    .then(response => response.json())
    .then(json => {
      L.geoJson(json, {
        style: customStyle,
        pointToLayer: function (feature, latlng) {
          if (iconUrl) {
            var customIcon = L.icon({
              iconUrl: iconUrl,
              iconSize: [30, 30],
              iconAnchor: [15, 15]
            });
            return L.marker(latlng, { icon: customIcon });
          } else {
            return L.circleMarker(latlng, {
              radius: 6,
              fillColor: "#ff7800",
              color: "#000",
              weight: 1,
              opacity: 1,
              fillOpacity: 0.8
            });
          }
        },
        onEachFeature: allowPopupFor(map)
          ? function (feature, layer) {
              // Simple default content — customize if you want a title/ordering
              let html = "";
              for (let k in feature.properties) {
                html += `<p><strong>${k}</strong>: ${feature.properties[k]}</p>`;
              }
              if (html) {
                layer.bindPopup(html, {
                  className: popupClassFor(map),
                  maxWidth: 320,
                  autoPanPaddingTopLeft: [20, 20],
                  autoPanPaddingBottomRight: [20, 20]
                });
              }
            }
          : undefined
      }).addTo(map);
    })
    .catch(error => console.error(`Error loading ${url}:`, error));
}
  


// tern steps points style 
function styleStepPoints(feature) {
  return {
    radius: 6,
    fillColor: "#34547e",
    color: "#40699e",
    weight: 1,
    opacity: 1,
    fillOpacity: 0.9
  };
}

// Load data layers onto the maps
// style breeding range layer
getData(breedingMap, 'data/tern/breeding-range.geojson', null, {
  color: "none",  
  stroke: "false",        // outline color
  weight: 2,                 // outline thickness
  fillColor: "#6db277",      // fill color
  fillOpacity: 0.6           // fill transparency
});

// get first points layer
getData(pointsMap, 'data/tern/tern-points-down.geojson', null, styleTernPointsDown);

// get climate zone data + style 
getData(climateMap, 'data/tern/climate-zones.geojson', null, styleClimateZone);

// style non breeding range layer
getData(nonBreedingMap, 'data/tern/non-breeding-range.geojson', null, {
  color: "none",  
  stroke: "false",        // outline color
  weight: 2,                 // outline thickness
  fillColor: "#6db277",      // fill color
  fillOpacity: 0.6           // fill transparency
});

// get second points layer
getData(points2Map, 'data/tern/tern-points-up.geojson', null, styleTernPointsUp);


getData(stepMap1, 'data/tern/step1.geojson', null, styleStepPoints);
getData(stepMap2, 'data/tern/step2.geojson', null, styleStepPoints);
getData(stepMap3, 'data/tern/step3.geojson', null, styleStepPoints);
getData(stepMap4, 'data/tern/step4.geojson', null, styleStepPoints);
getData(stepMap5, 'data/tern/step5.geojson', null, styleStepPoints);
getData(stepMap6, 'data/tern/step6.geojson', null, styleStepPoints);
getData(stepMap7, 'data/tern/step7.geojson', null, styleStepPoints);
getData(stepMap8, 'data/tern/step8.geojson', null, styleStepPoints);
getData(stepMap9, 'data/tern/step9.geojson', null, styleStepPoints);
getData(stepMap10, 'data/tern/step10.geojson', null, styleStepPoints);
getData(stepMap11, 'data/tern/step11.geojson', null, styleStepPoints);
getData(stepMap12, 'data/tern/step12.geojson', null, styleStepPoints);
getData(stepMap13, 'data/tern/step13.geojson', null, styleStepPoints);
getData(stepMap14, 'data/tern/step14.geojson', null, styleStepPoints);
getData(stepMap15, 'data/tern/step15.geojson', null, styleStepPoints);
getData(stepMap16, 'data/tern/step16.geojson', null, styleStepPoints);
getData(stepMap17, 'data/tern/step17.geojson', null, styleStepPoints);
getData(stepMap18, 'data/tern/step18.geojson', null, styleStepPoints);
getData(stepMap19, 'data/tern/step19.geojson', null, styleStepPoints);
getData(stepMap20, 'data/tern/step20.geojson', null, styleStepPoints);
getData(stepMap21, 'data/tern/step21.geojson', null, styleStepPoints);


// Shared style for ALL sea-ice polygons
const seaIceStyle = {
  color: "none", 
  stroke: "false",     // keep as you have it
  weight: 2,          // (used only if color/stroke is active)
  fillColor: "#8e8e8e",
  fillOpacity: 0.6
};

// sea-ice trace (uses the same style)
getData(iceMap, 'data/tern/sea-ice-trace.geojson', null, seaIceStyle);


// sea-ice time slices (all share seaIceStyle)
getData(iceMap1,  'data/tern/sea-ice-1979.geojson', null, seaIceStyle);
getData(iceMap2,  'data/tern/sea-ice-1985.geojson', null, seaIceStyle);
getData(iceMap3,  'data/tern/sea-ice-1990.geojson', null, seaIceStyle);
getData(iceMap4,  'data/tern/sea-ice-1995.geojson', null, seaIceStyle);
getData(iceMap5,  'data/tern/sea-ice-2000.geojson', null, seaIceStyle);
getData(iceMap6,  'data/tern/sea-ice-2005.geojson', null, seaIceStyle);
getData(iceMap7,  'data/tern/sea-ice-2010.geojson', null, seaIceStyle);
getData(iceMap8,  'data/tern/sea-ice-2015.geojson', null, seaIceStyle);
getData(iceMap9,  'data/tern/sea-ice-2020.geojson', null, seaIceStyle);
getData(iceMap10, 'data/tern/sea-ice-2025.geojson', null, seaIceStyle);





// call mapbox 
mapboxgl.accessToken = 'pk.eyJ1Ijoic21pY2hhbHNraSIsImEiOiJjbDl6d2s0enYwMnI1M29uMDhzNXB0NTRlIn0.c1_vy157AkEEGNIfyQI9YQ';

function initMapboxTerrainMap(containerId, centerCoords, zoom = 10) {
  const map = new mapboxgl.Map({
    container: containerId,
    style: 'mapbox://styles/mapbox/satellite-v9',
    projection: 'globe',
    center: centerCoords,
    zoom: zoom,
    pitch: 60,
    bearing: -10,
    antialias: true
  });

  // Disable scroll-to-zoom so page scrolling works normally
  map.scrollZoom.disable();

  // Add zoom-only buttons (no compass)
  map.addControl(new mapboxgl.NavigationControl({ showCompass: false }), 'top-left');


  map.on('load', () => {
    map.addSource('mapbox-dem', {
      type: 'raster-dem',
      url: 'mapbox://mapbox.terrain-rgb',
      tileSize: 512,
      maxzoom: 14
    });

    map.setTerrain({ source: 'mapbox-dem', exaggeration: 1.5 });

    map.addLayer({
      id: 'sky',
      type: 'sky',
      paint: {
        'sky-type': 'atmosphere',
        'sky-atmosphere-sun': [0.0, 0.0],
        'sky-atmosphere-sun-intensity': 15
      }
    });
  });
}

// call mapbox gl js locations
initMapboxTerrainMap('glMapAlaska', [-166.527, 53.83], 13);
initMapboxTerrainMap('glMapOregon', [-124.29, 40.22], 14);
initMapboxTerrainMap('glMapEcuador', [-78.55, -0.14], 13);
initMapboxTerrainMap('glMapAntarctica', [-63.0333, -65.4], 6);







/* arrow controls */
window.addEventListener("scroll", function () {
  const prompt = document.getElementById("scrollPrompt");
  if (prompt) {
    prompt.style.transition = "opacity 0.5s ease";
    prompt.style.opacity = "0";
    setTimeout(() => prompt.style.display = "none", 500);
  }
});


// Windy map interaction toggle buttons with styles and active state
document.addEventListener('DOMContentLoaded', function () {
  const windyMap = document.getElementById("windyMap");
  const iframeContainer = windyMap.querySelector(".iframe-container");

  const buttonContainer = document.createElement("div");
  buttonContainer.style.position = "absolute";
  buttonContainer.style.top = "10px";
  buttonContainer.style.left = "10px";
  buttonContainer.style.zIndex = "9999";
  buttonContainer.style.display = "flex";
  buttonContainer.style.gap = "10px";

  function createToggleButton(text) {
    const btn = document.createElement("button");
    btn.innerText = text;
    btn.className = "windy-toggle-btn";
    return btn;
  }

  const enableBtn = createToggleButton("Enable Map Interaction");
  const disableBtn = createToggleButton("Disable Map Interaction");

  function setActive(btn) {
    enableBtn.classList.remove("active");
    disableBtn.classList.remove("active");
    btn.classList.add("active");
  }

  enableBtn.addEventListener("click", () => {
    iframeContainer.style.pointerEvents = "auto";
    setActive(enableBtn);
  });

  disableBtn.addEventListener("click", () => {
    iframeContainer.style.pointerEvents = "none";
    setActive(disableBtn);
  });

  // Set initial state
  disableBtn.classList.add("active");
  iframeContainer.style.pointerEvents = "none";

  buttonContainer.appendChild(enableBtn);
  buttonContainer.appendChild(disableBtn);
  windyMap.appendChild(buttonContainer);
});

 


// Chart.js: Migration distances by species
document.addEventListener('DOMContentLoaded', function () {
  const chartCanvas = document.getElementById('migrationChart');
  if (!chartCanvas) return;

  const ctx = chartCanvas.getContext('2d');

  const chart = new Chart(ctx, {
    type: 'bar',
    data: {
      labels: ['Arctic Tern', 'Great White Shark', 'Leatherback Sea Turtle', 'Monarch Butterfly', 'Wildebeest'],
      datasets: [{
        label: 'Annual Migration Distance (miles)',
        data: [44000, 12400, 7400, 5000, 1000],
        backgroundColor: 'rgba(177, 224, 255, 0.6)',
        borderColor: 'rgba(93, 173, 226, 1)',
        borderWidth: 1
      }]
    },
    options: {
      responsive: true,
      plugins: {
        legend: {
          labels: {
            color: '#333',
            font: {
              size: 14,
              family: 'bell'
            }
          }
        },
        tooltip: {
          backgroundColor: '#f4fbff',          // soft ice-blue like climate popup
          borderColor: '#b9e2ff',
          borderWidth: 1,
          cornerRadius: 12,                   // rounded corners
          titleColor: '#2b4a63',
          bodyColor: '#2b4a63',
          titleFont: {
            size: 14,
            weight: 'bold',
            family: 'bell'
          },
          bodyFont: {
            size: 12,
            family: 'bell'
          },
          callbacks: {
            label: function(context) {
              return `${context.dataset.label}: ${context.formattedValue} mi`;
            }
          }
        }
      },
      scales: {
        x: {
          ticks: {
            color: '#333',
            font: {
              size: 13,
              family: 'bell'
            }
          },
          grid: {
            display: false
          }
        },
        y: {
          beginAtZero: true,
          title: {
            display: true,
            text: 'Miles',
            color: '#333',
            font: {
              size: 14,
              family: 'bell'
            }
          },
          ticks: {
            color: '#333',
            font: {
              size: 12,
              family: 'bell'
            }
          },
          grid: {
            color: 'rgba(200, 200, 200, 0.3)',
            borderDash: [4, 4]
          }
        }
      }
    }
  });
});
